<%= form_for(@club_announcement) do |f| %>
  <% if @club_announcement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@club_announcement.errors.count, "error") %> prohibited this club_announcement from being saved:</h2>

      <ul>
      <% @club_announcement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :club_id %><br>
    <%= f.number_field :club_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
